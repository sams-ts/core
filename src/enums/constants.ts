
export enum MiddlewareScope {
    GLOBAL,
    CONTROLLER,
    HANDLER
}

export enum ParamDecorators {
    BODY = "body",
    QUERY = "query",
    PARAMS = "params",
    REQ = "req",
    RES = "res"
}